{
  "name": "Run Tests After Task",
  "version": "1.0.0",
  "description": "Run Go tests after completing a spec task to verify nothing broke",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "go test ./... -short -count=1 2>&1 | tail -20"
  }
}
